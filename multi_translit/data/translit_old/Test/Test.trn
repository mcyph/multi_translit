provides("Test Formatting Engine %s", "Latin", "IPA", true)

if before(B): Z = allforms(before|)
if after(A): Z = allforms(|after)
s = self(self), initial(initial), final(final), medial(medial)

BEGIN Modifiers(Output)
    # Add in IPA format [whatever]
    #from format("/%s/")
    #to format("/%s/")
    from format("[%s]")
    to format("[%s]")
    #from strip("[")
    #to strip("]")
END Modifiers
